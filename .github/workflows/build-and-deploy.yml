name: build-and-deploy

on:
  pull_request:

jobs:
  build:
    runs-on: ${{ fromJSON('["Windows", "ARM64"]') }}
    steps:
      - name: Say hello
        shell: bash
        run: |
          echo hello